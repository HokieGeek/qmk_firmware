SRC += hokiegeek.c

ifndef COMMAND_ENABLE
	COMMAND_ENABLE = no
endif

ifndef NKRO_ENABLE
	NKRO_ENABLE = yes
endif

ifndef BACKLIGHT_ENABLE
	BACKLIGHT_ENABLE = no
endif

ifndef RGBLIGHT_ENABLE
	RGBLIGHT_ENABLE = no
endif

ifndef AUDIO_ENABLE
	AUDIO_ENABLE = no
endif
